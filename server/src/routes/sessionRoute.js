const r=require('express').Router();const c=require('../controllers/sessionController');const{protect,restrictTo}=require('../middlewares/authMiddleware');r.use(protect);r.get('/classes/:classId',c.list);r.post('/classes/:classId/generate',restrictTo('admin'),c.generate);r.post('/classes/:classId',restrictTo('admin'),c.create);r.get('/:sessionId/attendance',restrictTo('admin'),c.roster);r.patch('/:sessionId/attendance',restrictTo('admin'),c.mark);r.get('/transfers/options',restrictTo('user'),c.transferOptions);r.get('/transfers/list',c.transfers);r.post('/transfers',restrictTo('user'),c.requestTransfer);r.patch('/transfers/:id',restrictTo('admin'),c.process);module.exports=r;
